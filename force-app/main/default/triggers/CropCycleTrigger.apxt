trigger CropCycleTrigger on Crop_Cycle__c (before insert, before update) {
    if (Trigger.isBefore && (Trigger.isInsert || Trigger.isUpdate)) {
        CropCycleTriggerHandler.handleBeforeInsert(Trigger.new);
    }
}